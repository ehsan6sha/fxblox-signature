{
  "name": "fxblox-signature",
  "version": "1.0.0",
  "description": "Web portal for generating FxBlox signatures using MetaMask and other Web3 wallets",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "dependencies": {
    "@functionland/fula-sec-web": "*",
    "ethers": "^6.11.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "vite": "^5.0.0",
    "gh-pages": "^6.1.0"
  }
}
